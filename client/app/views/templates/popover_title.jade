input.calendarcombo(value=calendar)

input.input-desc(tabindex="1",
                 type="text",
                 value=description,
                 placeholder=t("summary"))

input.input-place(tabindex="2",
                  type="text",
                  value=place,
                  placeholder=t("Place"))


button.close(title=t('close')) &times;
i.remove.icon-trash(title=t('delete'))
