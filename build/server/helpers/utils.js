// Generated by CoffeeScript 1.10.0
var client, log;

client = require('./client');

log = require('printit')({
  prefix: "utils",
  date: true
});

module.exports.exist = function(req, res) {
  var id;
  id = req.params.id;
  return client.get("data/exist/" + id + "/", function(error, response, body) {
    if (error) {
      return res.status(500).send(error);
    } else if ((body == null) || (body.exist == null)) {
      return res.status(500).send(new Error("Data system returned invalid data."));
    } else {
      return res.status(200).send(body.exist);
    }
  });
};
